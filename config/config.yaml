vector_db:
  index_name: "trading-bot-db"

retriever:
  top_k: 3
  score_threshold: 0.5

embedding_model:
  provider: "google"
  model_name: "models/gemini-embedding-001"

llm:
  google:
    provider: "google"
    model_name: "gemini-3.1-flash-lite"
  groq:
    provider: "groq"
    model_name: "deepseek-r1-distill-llama-70b"

tools:
  tavily:
    max_results: 5